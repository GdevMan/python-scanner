# python-scanner
a python port scanner i made

## How to use
1. Git clone the repo
2. cd in the ```python-scanner``` directory
3. run python port_scanner.py [ target ] [ options ]
## What it can do
- Can use threading
- Can detect banners
- Can find vulnerbilities (Still in beta)
